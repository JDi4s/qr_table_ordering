<!-- app/views/staff/settings/edit.html.erb  (FULL FILE — create) -->
<h1>Settings</h1>

<%= form_with url: staff_settings_path, method: :patch do %>
  <p>
    <label>
      <input type="checkbox" name="user[staff_sound_enabled]" value="1" <%= "checked" if current_user.staff_sound_enabled %>>
      Play sound when a new order arrives
    </label>
  </p>

  <button type="submit">Save</button>
<% end %>
