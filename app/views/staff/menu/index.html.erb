<h1>Menu</h1>

<% if current_user.manager? %>
  <div class="toolbar">
    <%= link_to 'Criar categoria', new_staff_category_path, class: 'button' %>
    <%= link_to 'Criar produto', new_staff_menu_item_path, class: 'button secondary' %>
  </div>
<% end %>

<% @categories.select(&:root?).sort_by(&:name).each do |category| %>
  <%= render 'staff/menu/category', category: category, level: 0 %>
<% end %>
