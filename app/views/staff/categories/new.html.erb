<h1>New Category</h1>

<%= form_with model: [:staff, @category] do |f| %>
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit "Create" %>
  </div>
<% end %>

<p><%= link_to "Back", staff_categories_path %></p>
